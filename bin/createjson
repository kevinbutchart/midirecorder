#!/bin/bash
fname="$1"
infoname=${fname%.mid}.json

duration=$(duration "$fname")
name=$(basename "$fname") 
echo "{ \"fullname\" : \"$fname\", \"name\" : \"$name\", \"duration\" : \"$duration\" }" > $infoname
