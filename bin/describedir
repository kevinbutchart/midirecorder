#!/bin/bash
DIR="$1"
DIRNAME=$(basename "$1")
createjson "$DIR"
FILES=$(ls $1/*.json)
FILES=$(basename -a $FILES | sort -n)
str="{\"date\":\"$DIRNAME\",\"recordings\":["
for f in $FILES
do
   str+="$(cat $DIR/$f),"
done
str=${str%?}"]}"
echo $str
